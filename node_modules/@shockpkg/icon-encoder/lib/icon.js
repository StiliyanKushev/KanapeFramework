"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.Icon = void 0;

var _upngJs = _interopRequireDefault(require("upng-js"));

/**
 * Icon constructor.
 */
class Icon extends Object {
  constructor() {
    super();
  }
  /**
   * Decode PNG data to RGBA image.
   *
   * @param data PNG data.
   * @returns Image data.
   */


  _decodePngToRgba(data) {
    const image = _upngJs.default.decode(data);

    return {
      width: image.width,
      height: image.height,
      data: new Uint8Array(_upngJs.default.toRGBA8(image)[0])
    };
  }
  /**
   * Encode RGBA image to PNG data.
   *
   * @param imageData Image data.
   * @returns PNG data.
   */


  _encodeRgbaToPng(imageData) {
    return Buffer.from(_upngJs.default.encode([imageData.data.buffer], imageData.width, imageData.height, 0, [], true));
  }
  /**
   * Encode icon.
   *
   * @returns Encoded icon.
   */


}

exports.Icon = Icon;
//# sourceMappingURL=icon.js.map
