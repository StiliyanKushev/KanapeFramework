/// <reference types="node" />
import { IPngIhdr } from './types';
/**
 * Read PNG IHDR data.
 *
 * @param data PNG data.
 * @returns PNG IHDR.
 */
export declare function pngIhdr(data: Readonly<Buffer>): IPngIhdr;
