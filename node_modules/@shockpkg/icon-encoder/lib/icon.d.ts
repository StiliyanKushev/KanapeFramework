/// <reference types="node" />
import { IImageData } from './types';
/**
 * Icon constructor.
 */
export declare abstract class Icon extends Object {
    constructor();
    /**
     * Decode PNG data to RGBA image.
     *
     * @param data PNG data.
     * @returns Image data.
     */
    protected _decodePngToRgba(data: Readonly<Buffer>): IImageData;
    /**
     * Encode RGBA image to PNG data.
     *
     * @param imageData Image data.
     * @returns PNG data.
     */
    protected _encodeRgbaToPng(imageData: Readonly<IImageData>): Buffer;
    /**
     * Encode icon.
     *
     * @returns Encoded icon.
     */
    abstract encode(): Buffer;
}
